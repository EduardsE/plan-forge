# PlanForge — implementation progress

Implementation of PlanForge from the design mockups (product context: see "What we're building" in `CLAUDE.md`) (`design/planforge-mockups.html` — open it in a browser to see all 4 screens side by side; screenshots in `design/screen-*.png`).

**How to use this file:** work on ONE task at a time, in order. Check off items as they land. Each task is sized to fit comfortably in a single Claude session. The mockups define the *look*, not the implementation — their specific numbers and furniture are sample data, never hardcoded artwork.

## The design at a glance

A room/floor-plan editor. One full-screen workspace with four states shown in the mockups:

| Screen | What it shows |
|--------|---------------|
| 1a | 3D isometric "dollhouse" view, an object selected (context toolbar: rotate/duplicate/delete + dimensions) |
| 1b | Top-down 2D floor plan: walls, door arc, window, furniture footprints, dimension lines, room-area tooltip |
| 1c | Draw mode: wall-drawing tool stack, placed corners, segment length labels, inline length input, snap guides, cm/m units toggle |
| 1d | Objects panel: searchable furniture catalog with category chips, drag-out card, ghost placement footprint in 3D |

Shared chrome on every screen: dark navy left nav rail (Dashboard / Draw / Furnish / Objects / Views / Settings / avatar), project title + contextual status line top-left, floating undo/zoom toolbar, bottom-left 2D↔3D + grid/snap/fullscreen controls, bottom-right contextual readout chip (orbit/zoom, scale/grid, or snap mode).

**Visual language:** Space Grotesk (UI) + IBM Plex Mono (numeric readouts/chips), navy `#0F1B3D` / `#0C1430`, teal-cyan accent `#5EEAD4`→`#0EA5E9`, light blue-grey canvas `#F3F6FA` with grid lines, warm beige/terracotta room rendering, pill-shaped chips, big soft shadows.

## Done: Phases 0–3 (verified headless end-to-end 2026-07-10 — all four lenses match the mockups, 121 unit tests + Biome clean)

- **Phase 0 — Foundation:** design tokens in `src/styles.css`, planner route at `/`, view-mode state (`ViewMode` in `src/lib/view-mode.ts`: `"3d" | "2d" | "draw" | "objects"`, named after screens 1a–1d).
- **Phase 1 — App shell & chrome:** nav rail, header + contextual status line, floating undo/zoom toolbar, 2D|3D pill + grid/snap/fullscreen group, bottom-right readout chip, cm/m units toggle. On screen 1d the left chrome shifts to `left:404px` to clear the objects panel.
- **Phase 2 — Room model & both lenses:** rendering-agnostic geometry model (`src/lib/model/`), sample living-room fixture measured from the mockups (6.40 × 5.20 m → computed 33.28 m²), one R3F canvas with an animated dolly-zoom 2D↔3D camera handoff, warm 3D dollhouse with camera-facing wall cutaway, architectural 2D plan (door arc, window symbol, dimension lines, area card), 3D selection with rotate/duplicate/delete chip.
- **Phase 3 — Editing flows:** draw mode (corner placement, 90°/axis snapping with guides + chips, per-segment length pills, inline length input, ⏎-close into the model, cm/m everywhere) and the objects panel (24-item catalog, search + category chips, pointer-drag onto the floor with ghost + wall-snap guides, drop inserts real furniture).

Per-task implementation notes live in the git history (one commit per task).

## Phase 4 — Editing depth & persistence

Ordered by value; re-order freely if a dependency argues for it.

- [x] **Drag-to-move placed furniture** (3D lens first, like selection was): pointer-drag a selected item across the floor reusing `snapPlacement`'s quantize / outline clamp / wall-flush snap + guide pills; selection chip follows live; esc during the drag restores the original position. Watch the R3F drag-vs-orbit guard (`event.delta`).
- [x] **Selection & manipulation in the 2D lens:** picking on plan footprints, hover/selected styling that suits the plan look, the same chip (rotate/duplicate/delete + dimensions) — selection id already survives lens switches, so lift it out of `PlannerCanvas` only if this task needs it elsewhere. Landed with drag-to-move too, via the move-drag session extracted from the 3D lens (`src/components/move-drag.tsx`).
- [ ] **Undo/redo history:** wire the toolbar buttons + ⌘Z/⇧⌘Z to a bounded snapshot stack over the room model (plain data — snapshot per mutation: add/move/rotate/duplicate/delete, outline close). Redo stays dimmed until an undo happens (mockup 1a shows it dimmed).
- [x] **Persistence:** the model is already plain JSON — autosave to localStorage on mutation, hydrate on load, and make the header's "saved just now" real (it's fake copy today). Include unit + room name; a "new room" escape hatch clears it. Landed as `src/lib/persistence.ts` (versioned payload, paranoid deserialize — malformed saves hydrate as "no save"); the route hydrates post-mount (SSR has no localStorage) and skips re-saving what it just loaded so reloads keep the honest saved-at time; "New room" (header status row) confirms, clears to an empty outline, and drops into draw mode.
- [x] **Door & window placement tools:** add/move openings — click a wall to insert, drag along it with offset snapping + distance-to-corner readouts, flip a door's hinge, delete. Opening heights stay `room-scene.ts` constants until a properties panel exists. Landed on the 2D lens: an Add door / Add window stack (`opening-tool-stack.tsx`, draw-stack styling) arms a tool whose invisible wall strips preview a ghost + corner pills and insert on click (0.9 / 1.2 m defaults); openings then pick and drag along their wall like footprints (`plan-openings.tsx`), with a chip for flip-hinge/delete. Pure math in `opening-place.ts` (`slideOpening` quantizes to the placement grid and clamps into the nearest gap between neighboring openings — overlaps impossible) and `model/openings.ts`; `WallHole` now carries its opening's id for picking.
- [x] **Edit the existing room in draw mode (Draw ≠ new room):** entering draw mode should reopen the *current* outline as an editable draft, not a blank canvas — clicking "Draw" in the nav means "edit this room," never "throw it away and start over." Today `setViewMode("draw")` starts an empty draft that *replaces* the room on close (`closeDraft` wipes openings + furniture); flip that default so the draft seeds from `room.outline`. Corners are draggable (same quantize / axis guides / length pills as fresh drawing), and you can add/insert or drag out new wall segments to reshape the outline (split a wall at a click, drag the new corner). Committing keeps the openings and furniture instead of clearing them — re-anchor openings to their host wall (index + offset survive the reshape), drop or keep each furniture piece by whether it still fits inside the new outline. Starting from a blank outline is *only* the explicit **New room** button (already in the header status row) — the one gesture that clears everything. An already-empty outline (right after New room) naturally falls back to today's from-scratch corner placement, so fresh drawing is unchanged. Landed: the draft is now `{ corners, closed, openings }` (`src/lib/outline-edit.ts`) — closed drafts carry the openings so wall splits remap indices/offsets exactly at split time; commit (`applyOutlineDraft`) re-slides each opening into its wall via `slideOpening` (drops what no longer fits) and keeps furniture whose rotated footprint (`footprintCorners`) stays inside the outline (`pointInOutline`, 1 mm boundary tolerance for flush placements). ⏎ applies and jumps to 2D; leaving draw mode via any nav applies too (edits are never silently lost); esc reverts the session. Closed-loop length pills use `setClosedSegmentLength`: the shift propagates through perpendicular walls and the first parallel wall absorbs it (a rectangle edits like a rectangle). Corner drags are a `move-drag`-style window-listener session whose esc/⏎ handler registers capture-phase and stops propagation, so the route's draft-wide keys sit drags out; wall-split clicks refuse within 0.25 m of a corner (> the 0.18 m corner pick radius, so a no-move press on a corner can't split).
- [x] **Draw tool stack, rest of it:** rect tool (two clicks → rectangular outline); then implement or delete the polygon/split stubs — inert buttons shouldn't survive the phase. Landed: `rectangleOutline`/`snapRectPoint` in `src/lib/draw.ts` (CW corners from two opposite clicks, null when a side collapses). The rect tool arms a crosshair in `DrawScene`; the first click sets an anchor, the second calls `onPlaceRect`, which replaces the draft with a fresh **closed** rectangle and hands off to Select so it drags/edits like any closed draft (openings drop — they belonged to a different outline; furniture that still fits is kept at commit by `applyOutlineDraft`). The between-clicks preview (`RectPreview`) is a **solid** cyan `<Line>` loop + width/height pills, deliberately not dashed: a dashed loop chops into ~50 segment quads that stall drei's `<Line>` on this GPU stack (see gotcha). The two stub buttons were **deleted** — "Arc wall" (curved walls aren't in the straight-segment model) and "Split wall" (wall splitting already lives in the closed-draft reshaping flow); `DrawTool` is now just `select | wall | rect`. Verified headless against the production build (dev server unusable — see gotcha): stack shows exactly Select/Draw walls/Rectangle room, two clicks yield a 3.00×2.20 m rectangle (4 length pills), tool flips to Select, ⏎ commits to 2D, no console errors.
- [x] **Grid / snap / fullscreen buttons do something:** grid toggles the in-scene drei `<Grid>`, snap disables draw quantize/axis-lock and placement wall-snap, fullscreen requests browser fullscreen on the workspace; toggles live in view state (they're hardcoded-lit today). Landed: `gridVisible`/`snapEnabled` state in the route (both default on) plus a `workspaceRef`; `ViewControls` now takes the two toggles + `onFullscreen` and lights grid/snap from state (fullscreen stays a plain action, no `aria-pressed`). Grid gates the `<Grid>` render in `planner-canvas`. Snap threads a trailing `snap = true` param into the pure functions — `snapDraftPoint`/`snapCornerDrag` return the raw cursor (no axis-lock, no quantize) and `snapPlacement` passes the cursor through unquantized with the flush tolerance dropped to 0 (**containment/clamp still applies**, so free placement can't shove furniture through a wall) and no guide pills — carried through `DrawScene`/`OutlineEditLayer`→`CornerDragSession`, both `MoveDragSession` lenses, and `PlacementGhost`. Fullscreen toggles `requestFullscreen()`/`exitFullscreen()` on the `.workspace-canvas` pane (nav rail drops away, workspace toolbars ride along). Verified headless: grid button flips the canvas pixels (grid gone), snap button flips its `aria-pressed`, fullscreen lands `document.fullscreenElement` on `workspace-canvas`, no console errors.
- [x] **Object-to-object snapping:** the objects-panel footer already promises "snaps to walls and other objects" — flush/edge-align guides between the ghost (and later, dragged furniture) and placed items, same pill style as the wall guides. Landed: `snapPlacement` takes an `obstacles: Obstacle[]` (each placed item's rotated-footprint AABB, via `furnitureObstacle`) and folds the neighbours' near faces into the same nearest-line pipeline as the walls — so a placed item snaps flush against a neighbour's edge and its clearance renders as the identical teal pill (`SnapGuides`). The key correctness gate is `objectFaces`: an item's face only captures when the mover *overlaps it on the other axis* **and** already sits wholly to one side on this one, so a flush-beside neighbour never shoves the mover along the axis it already overlaps (which would drive it *into* the neighbour). Wired into the placement ghost (obstacles = every placed item — a fresh drop isn't in the room yet) and both move-drag lenses (obstacles = every item bar the dragged one, memoised on `[room.furniture, drag?.id]`, read through a ref so the per-move room churn doesn't resubscribe the drag listeners). Verified headless: dragging the rug toward the desk counts the clearance down (0.65→0.34 m) then snaps flush; the perpendicular axis stays put. Rugs are still snap targets — a category-level "no-snap" exclusion can wait for the wall-mounted-items / collision tasks.
- [x] **Wall-mounted items:** picture frame / wall clock currently drop as thin standing boxes — model a wall-mount placement (host wall + offset + height), snap to walls during drag, render on the wall in both lenses. Landed: `FurnitureItem` gains an optional `mount { wallIndex, offset, elevation }` (`src/lib/model/wall-mount.ts`); when set, `position`/`rotation` are *derived* from the wall (`deriveMountTransform` — centered along the wall, pushed `depth/2` into the room so the back sits flush, width axis turned to the wall) and kept in sync on every mutation, so the 2D footprint / selection / containment code reuses them unchanged. Placement math (`src/lib/mount-place.ts`, parallel to `opening-place.ts`): `mountAt` snaps a floor cursor to the nearest wall that fits (offset quantized via `slideOpening`, next-nearest fallback), `reanchorMount` re-homes a mount to its geometrically nearest wall after an outline reshape (dropped if it no longer fits). Wall items (catalog category `wall-items`, `isWallItem`) route to `WallMountGhost` (a standing dashed rect at mount elevation + the opening-style corner guides) on drop, and to a mount branch in the shared `MoveDragSession` (now patch-based `FurnitureUpdate` — floor drags patch position, wall drags also rotation + mount; esc restores both). 3D hangs the body at `elevation` with no floor shadow; the selection chip anchors above it and hides Rotate (mounts align to their wall). Default center heights per item (frame 1.5 m, clock 1.9 m). Persistence validates the optional mount; the sample room ships a frame on the left wall. Verified headless: sample frame renders on the wall in both lenses, dragging a Wall Clock card mounts it to the nearest wall (guide pill = far-corner gap), and dragging the placed frame re-mounts it across walls — no console errors.
- [x] **Collision awareness:** placement clamps to the outline but rotate/duplicate bypass it (rotating the 2.2 m desk near a wall pokes through). Keep footprints inside the outline on every mutation; tint overlapping footprints as a warning rather than hard-blocking. Landed in `src/lib/collision.ts`: `containFurniture` re-runs the containment half of `snapPlacement` (snap off — bounds clamp + non-axis-wall push-in, no quantize/flush) for an item's *current* rotation, and `containRoomFurniture(room, id)` re-contains just the mutated item; wired into the canvas's `rotateItem`/`duplicateItem` (mounts pass through, anchored to their wall). Overlap is soft: `overlappingFurnitureIds` (SAT over rotated footprints, 1 cm penetration margin so flush-snapped neighbours don't false-warn; rugs and wall-mounts excluded — furniture is *meant* to sit on a rug) drives a red tint (`#e0533a`) in both lenses — 2D footprint fill+stroke, 3D body color mixed toward the warning red (selection cyan still wins the stroke so a selected+overlapping item reads as both). Verified headless end-to-end: rotating the sample desk 90° near the top wall clamps its center 0.73 → 1.1 m (top edge flush at 0.000, was −0.37) and tints the desk + the chair it now overlaps in both 2D and 3D; rug/plant/cabinet/shelf stay their warm tones; no console errors.
- [x] **Richer furniture meshes:** replace the box placeholders with simple composed primitives per catalog category (sofa back/arms, table legs, bed headboard, shelf boards…), colors/footprints still model-driven. Landed as `src/lib/furniture-parts.ts` (pure data, tested): `furnitureParts(catalogId, footprint)` returns a list of box/cylinder/sphere parts sized **proportionally** from the footprint, in bottom-relative local space where **+z is the item's front** (`deriveMountTransform` puts a wall item's back at −z; the sample credenza/shelf at rotation 90 then face into the room — the sample desk chair flipped to rotation 180 to face its desk, since facing became visible). Specific ids get tailored builds (desk = side panels + top at 0.66 h + navy monitor block, mockup 1a's silhouette; leaning floor mirror and clock hands use the parts' optional euler rotation), unknown ids fall back per category, then to the plain footprint box; the plant pot/foliage special case folded into the same system. `room-scene.tsx` renders each part as one lambert mesh plus, when active, an inflated copy (`partHullScale`, per-part in local axes so rotated parts keep a uniform rim) — replacing the three hand-written body branches. Colors moved into the lib (`FURNITURE_COLORS` + `mixHex` tone helpers); the warning tint still lerps every part toward red. Tests assert every part's rotated AABB stays inside the footprint (plants' bushy overhang exempt), backs sit at −z, and wall items stay back-flush. Verified headless against the production build: sample room composes in 3D, desk click-selects with the right readout, sofa card drops with per-part hull + chip, 2D plan untouched, zero console errors.

**Deliberately not planned:** Dashboard/Settings destinations, multi-room floors, material/texture pickers, export (PNG/PDF), mobile layout. Revisit after Phase 4.

## Conventions (everything builds on these)

- **All model lengths are meters**; cm/m is display-only (`src/lib/units.ts`). Furniture `rotation` is degrees CCW about the footprint center. Points are 2D plan coords — origin at the sample room's top-left, x right, y down — with heights as separate scalars; outlines wound clockwise (helpers tolerate either winding).
- **Walls are derived, never stored** (`wallsOf(outline)`); `Opening.wallIndex` indexes that derivation, plus `offset` along the wall, `width`, and optional `hinge` for the door swing. Wall/opening heights aren't modeled — they're mockup-measured constants in `src/lib/room-scene.ts` (wall 2.5 m × 0.1 thick, door head 2.05, window sill 0.36 / head 1.94).
- **Lib/component split everywhere:** pure math in `src/lib/*` with vitest coverage; rendering/interaction in `src/components/*`. The model stays renderer-agnostic (an SVG fallback for the 2D lens remains possible).
- **One R3F canvas** (`src/components/planner-canvas.tsx`), lazy-loaded client-only so three never runs during SSR. Both cameras stay mounted; drei `makeDefault` flips ortho↔perspective on the planView split (`"2d"`/`"draw"` ↔ `"3d"`/`"objects"`). The 2D↔3D switch is a ~600 ms dolly-zoom to a matched top-down endpoint before swapping to true ortho; scene presentation (`PlanScene` / `RoomScene` / `DrawScene`) switches on the same lagged `renderPlan` state, so the warm room stays up mid-flight.
- **Mockup numbers are sample data, never artwork:** 33.28 m² comes from `floorArea()`, the search placeholder counts real catalog items, selection readouts compute from footprints. The sample fixture was measured off the mockup canvases (110 px/m on 1b, 100 px/m on 1a). Known mockup inconsistencies deliberately not honored: 1a's chip text for the desk chair, 1d's guides-to-far-walls (we guide to nearest), decorative wall-art ticks.

## Gotchas (each cost real debugging time)

- drei `<Line>`: default alpha-to-coverage makes stroke quads resolve as opaque rectangles on this GPU stack — every plan stroke sets `alphaToCoverage={false}`, and dashes are pre-chopped into segments (`dashedPolyline`) because the dashed material misrenders the same way.
- drei `<Outlines>` renders nothing under drei 10 / three r185 (its `screenspace` mode explodes into a screen-filling hull) — the selection highlight is a hand-rolled inverted hull (same geometry +2 cm rim, cyan `meshBasicMaterial`, `side={BackSide}`).
- R3F pointer guards: only events whose DOM target is the actual `<canvas>` may deselect or place (drei `<Html>` clicks arrive as "pointer missed" / raycast through to the plane), and clicks that travelled > 4 px are orbit drags or pans, not clicks.
- The plan camera's straight-down pose reads as polar 90° in OrbitControls' up-relative frame — don't clamp `maxPolarAngle` in plan view; and exactly-0 polar degenerates `lookAt`, use 0.01.
- The objects drag never enters R3F's event system (pointer went down on DOM) — the ghost raycasts window pointermoves onto the y=0 plane manually. Wall-snap tolerance there is world-space (0.3 m): screen-px tolerance is depth-dependent under a perspective camera (draw mode's ortho snapping *is* screen-px based).
- Only furniture raycasts in the 3D scene (walls/platform/dressing set `raycast={() => null}`) — that's what makes furniture clickable behind visible walls.
- OrbitControls' `enabled` prop flushes too late to stop a gesture that begins on the same pointerdown (it eats the first pointermoves as orbit — the camera visibly drifts ~2°). The furniture move drag disables the controls *instance* synchronously in its pointerdown handler (`makeDefault` publishes it as `state.controls`); the prop only holds the steady state.
- Textures are module-cached CanvasTextures — safe only because the canvas module is lazy-loaded client-only.
- Browser verification: headless script with real `page.mouse` input (per CLAUDE.md — the MCP browser hangs on inactive macOS sessions, and synthetic PointerEvents make OrbitControls throw `NotFoundError`). Benign console noise: a `THREE.Clock` deprecation warning (drei 10 + r185). **Verify against the production build, not `pnpm dev`:** the TanStack devtools server-console forwarding catches that `THREE.Clock` warning in a feedback loop and re-echoes it with a growing `[Server]` prefix — tens of thousands of console messages within seconds, which pegs the page main thread (Playwright's in-page checks starve → `waitForSelector`/clicks time out even though the element "resolved to visible") and had ballooned a redirected dev log to 34 GB (filled the disk, which then made *everything* janky). `pnpm build && pnpm preview` (port 4173) has no console forwarding and drives cleanly.
- drei `<Line>` with `segments` + a long dashed loop (e.g. a rectangle's ~50 dash quads) stalls the render on this GPU stack — the rect-tool preview uses a **solid** (non-`segments`) `<Line>` loop instead, the same plain-Line pattern the committed outline uses. Short dashed *open* segments (wall preview, alignment guides) are fine.
- A leftover `pnpm preview` keeps serving the *old* server bundle after a rebuild (the SSR HTML then references client chunk hashes that 404) — and `pkill -f "vite preview"` doesn't match pnpm's process name. Kill it by port before re-verifying: `lsof -tnP -iTCP:4173 -sTCP:LISTEN | xargs kill`.
- The `@tanstack/devtools-vite` plugin's click-to-source injection is turned **off** in `vite.config.ts` (`injectSource: { enabled: false }`): it stamps `data-tsd-source` on every JSX element, and R3F reads hyphenated props as nested paths (`data-tsd-source` → `object.data.tsd.source`), so the attribute threw "Cannot set data-tsd-source" and tripped the canvas error boundary whenever a scene element mounted (e.g. during a furniture drag). Don't re-enable it without scoping every three.js-rendering file out.

## History

- 2026-07-09: Mockups imported from claude.ai/design project `20be5088-…` into `design/`; upstream rename to PlanForge, AI command-bar dropped from scope; `design/planforge-mockups.html` is self-contained (canvases fixed 1920×1080 — the real app is responsive-ish, min desktop).
- 2026-07-09: Phases 2–4 replanned from "static canvas mimics" to a real geometry model rendered by react-three-fiber in both lenses — one scene graph, one raycaster, one coordinate space; the 2D plan is the same scene under a top-down ortho camera. This directly serves the product hinge: one model, instant lens switch.
- 2026-07-10: Phases 0–3 complete and verified end-to-end; Phase 4 planned (this task list), per-task notes compacted into the sections above (full detail in git history).
- 2026-07-10: Placement snapping (`src/lib/place.ts`) generalized past the axis-aligned assumption — non-90° walls previously contributed no snap face *and* weren't contained (the only containment was the outline's bounding box), so furniture slid straight out through an angled wall. Axis-aligned walls keep the fast per-axis path; non-axis walls now get a half-plane pass (`angledWalls`/`applyAngledWalls`) using each wall's inward normal + tangential span that both contains the footprint and flush-snaps it along the normal, with matching guides (`angledWallGuides`). Verified headless in a seeded 45°-cut room: furniture snaps flush to the diagonal wall and slides along it instead of escaping.
